package game;

import java.io.IOException;

public class Game {

	/*
	
	public static void main(String[] args) throws IOException
	{
		// make user choose a scenario and save in chosenScenario - fe
		int NoOfScenarios = 3;
	        int chosenScenario;
                
		String initialScenarioAsString = InitialScenario.getInitialScenario(chosenScenario);
		 
		// show user the initial scenario - fe
		
		String []questions = Questions.getQuestions(chosenScenario);
		String []answers = Questions.getAnswers(chosenScenario);
		
		// show all questions to user with checkboxes - fe
		boolean selectedQuestions = new boolean[9];
		
		// show yes/no answer for the questions selected by the user - fe
		
		// user answers the other remaining question with yes/no checkboxes
		boolean userAnswerCorrect;
		int score = 0;
		
		for (int i = 0; i < 8; i++)
		{
			if (selectedQuestions[i] == false)
			{
				// make user answer this question - fe
				
				
				if (userAnswerCorrect == true)
					score++;
			}
			
		} // for
		boolean decrypted = false;
		int tries = 0;
		if (score >= 4)
		{
			// tell user has gone through next stage - fe
			
			// show user the encrypted final scenario - fe
			String finalScenarioAsString = FinalScenario.getFinalScenario(scenarioChosen);
			String encryptedScenario = FinalScenario.getFinalScenarioEncrypted(scenarioChosen);
			String keyword = Questions.getKeyWord(scenarioChosen);
		    String hint = "The story has been encrypted using Caesar Cipher but with a keyword which you have to guess. The mixed up letters of the keyword are " + keyword;
		    // make user write decrypted message in a text box - fe
			while (decrypted == false)
			{
				// when user presses submit decryption tries++
			
				if (tries >= 3)
				{
					// say user has lost
				
					//show decrypted message
				    break;
				}
			
				if (decrypted == true)
				{
					// say user has won
				}
			}
		}
		else 
			//tell user lost - fe
	}*/
}

